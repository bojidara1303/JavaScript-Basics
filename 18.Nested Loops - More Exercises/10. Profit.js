function profit(data) {
    let coinsOneCount = Number(data[0]);
    let coinsTwoCount = Number(data[1]);
    let coinsFiveCount = Number(data[2]);
    let sum = Number(data[3]);

    let oneCounter = 0;
    let twoCounter = 0;
    let fiveCounter = 0;
    let result = 0;
    for (let i = 1; i <= coinsFiveCount; i++) {
        for (let k = 1; k <= coinsTwoCount; k++) {
            for (let l = 1; l <= coinsOneCount; l++) {
                
            }
        }
    }

}
profit(["3", "2", "3", "10"])
console.log("_______________________")
profit(["5", "3", "1", "7"])